---
postgres:
  image: postgres
  ports:
    - "5432:5432"
  environment:
    POSTGRES_USER: timesync
    POSTGRES_PASSWORD: pass

web:
  build: .
  ports:
    - "8000:8000"
  volumes:
    - .:/opt/timesync-node
  links:
    - postgres
